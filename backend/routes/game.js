const express = require('express');
const router = express.Router();
const Question = require('./../models/Question');

// route to submit the answer
router.post('/question/:quesId', async (req, res) => {
       
});

module.exports = router;